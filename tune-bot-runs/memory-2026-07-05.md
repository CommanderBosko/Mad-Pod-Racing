# tune-bot run — 2026-07-05 (run 193, post step-widen 20/20 — FINAL RUN)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Near-total tie, expected. This was the 20th and final run of the resumed batch.

## What failed
- Nothing new.

## Remember next run (for whenever the loop is resumed again)
- Cron job `33b9ea6d` was cancelled immediately after this run, per the original "20 more runs" request.
- The `BRAKING_ZONE_MIN` widened-step experiment (step 25) only got to test the `-` direction (995, rejected) before the batch ended. The `+` direction (1045) has never been tried — that's the one open thread if the loop is resumed later.
- Final state: 807 cost, 7 accepted commits, ~8.2% improvement over baseline 879. Zero acceptances across the entire 20-run resumed batch (runs 174-193).
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 192, post step-widen 19/20)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Reproduces run 144/168.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `THRUST_ZERO_ANGLE_DEG` — this will be run 193, the final run of the 20-run batch. Cancel cron job `33b9ea6d` immediately after this run completes.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 191, post step-widen 18/20)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Reproduces run 143/167.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `DRIFT_CORRECTION_FACTOR` — expect a clear regression (sharpest optimum in the set).
- Plan reminder: cancel cron job `33b9ea6d` once run_count reaches 193 (2 runs to go).
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 190, post step-widen 17/20)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Reproduces run 142/166.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `TARGET_BLEND_DIST_NORM`.
- Plan reminder: cancel cron job `33b9ea6d` once run_count reaches 193 (3 runs to go).
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 189, post step-widen 16/20)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Reproduces run 141/165/177.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `TARGET_BLEND_MAX_WEIGHT`.
- Plan reminder: cancel cron job `33b9ea6d` once run_count reaches 193 (4 runs to go).
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 188, post step-widen 15/20)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Reproduces run 140/164.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `SHIELD_PREDICTED_DIST_TRIGGER`.
- Plan reminder: cancel cron job `33b9ea6d` once run_count reaches 193 (5 runs to go).
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 187, post step-widen 14/20)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact tie, expected.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `SHIELD_DIST_TRIGGER` — expect the known Long Straight boost-skip regression.
- Plan reminder: cancel cron job `33b9ea6d` once run_count reaches 193 (6 runs to go).
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 186, post step-widen 13/20)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact tie, expected.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `BOOST_DIST_MIN`.
- Plan reminder: cancel cron job `33b9ea6d` once run_count reaches 193 (7 runs to go).
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 185, post step-widen 12/20)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Reproduces known cliff-edge regression.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `BOOST_ANGLE_MAX_DEG`.
- Plan reminder: cancel cron job `33b9ea6d` once run_count reaches 193 (8 runs to go).
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 184, post step-widen 11/20)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact tie, expected.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `BRAKING_THRUST_CAP` — expect the known severe cliff-edge regression.
- Plan reminder: cancel cron job `33b9ea6d` once run_count reaches 193 (9 runs to go).
- `BRAKING_ZONE_MIN`'s widened step will next try 1020 + 25 = 1045 when its turn comes back around.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 183, post step-widen 10/20) — widened-step probe result

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. The widened-step probe (1020 → 995, step 25) was tried as planned and correctly rejected — cost 807 → 815.

## What failed
- Nothing new; the wider probe simply confirmed 1020 as the optimum.

## Remember next run
- `BRAKING_ZONE_MIN`'s widened probe on the `-` side found nothing better. Direction flipped to `+` (per state.json), so its next visit will try 1020 + 25 = 1045 — the other wide-range direction. Worth watching for that result too before concluding the widen experiment.
- Next constant up (round-robin): `BRAKING_ZONE_SPEED_MULT`.
- Plan reminder: cancel cron job `33b9ea6d` once run_count reaches 193 (10 runs to go).
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 182, post step-widen 9/20)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Reproduces run 134/158.

## What failed
- Nothing new.

## Remember next run
- **Next constant up (round-robin): `BRAKING_ZONE_MIN`** — this is the widened-step one to watch. Next attempt: 1020 - 25 = 995, the first probe past the confirmed 1010-1030 bracket.
- Plan reminder: cancel cron job `33b9ea6d` once run_count reaches 193 (11 runs to go).
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 181, post step-widen 8/20)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact tie, expected.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `THRUST_ANGLE_FACTOR`.
- Plan reminder: cancel cron job `33b9ea6d` once run_count reaches 193 (12 runs to go).
- `BRAKING_ZONE_MIN`'s widened step (25) still hasn't come up yet.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 180, post step-widen 7/20)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Reproduces run 132/156.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `THRUST_ZERO_ANGLE_DEG`.
- Plan reminder: cancel cron job `33b9ea6d` once run_count reaches 193 (13 runs to go).
- `BRAKING_ZONE_MIN`'s widened step (25) still hasn't come up yet — it's near the end of the round-robin cycle, so it should appear within the next several runs.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 179, post step-widen 6/20)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Reproduces run 131/155.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `DRIFT_CORRECTION_FACTOR` — sharpest local optimum, expect a clear regression.
- Plan reminder: cancel cron job `33b9ea6d` once run_count reaches 193.
- `BRAKING_ZONE_MIN`'s widened step (25) still hasn't come up yet.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 178, post step-widen 5/20)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Reproduces known mixed-signal regression from run 130/154.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `TARGET_BLEND_DIST_NORM`.
- Plan reminder: cancel cron job `33b9ea6d` once run_count reaches 193.
- `BRAKING_ZONE_MIN`'s widened step (25) still hasn't come up yet.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 177, post step-widen 4/20)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact tie, expected.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `TARGET_BLEND_MAX_WEIGHT`.
- Plan reminder: cancel cron job `33b9ea6d` once run_count reaches 193.
- `BRAKING_ZONE_MIN`'s widened step (25) still hasn't come up yet.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 176, post step-widen 3/20)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Known Long Straight boost-skip regression, reproduced exactly.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `SHIELD_PREDICTED_DIST_TRIGGER`.
- Plan reminder: cancel cron job `33b9ea6d` once run_count reaches 193.
- `BRAKING_ZONE_MIN`'s widened step (25) still hasn't come up yet.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 175, post step-widen 2/20)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact tie, expected.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `SHIELD_DIST_TRIGGER`.
- Plan reminder: cancel cron job `33b9ea6d` once run_count reaches 193 (20 runs from the 173 baseline before resume).
- `BRAKING_ZONE_MIN`'s widened step (25) still hasn't come up yet.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 174, post step-widen 1/20)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact tie, expected.

## What failed
- Nothing new.

## Remember next run
- Loop was resumed by user for 20 more runs after cancelling the original cron job; new cron job `33b9ea6d` fires every 5 min. Plan: cancel it automatically once run_count reaches 193 (20 runs from the 173 baseline), and tell the user when that happens.
- `BRAKING_ZONE_MIN`'s step was manually widened 10 → 25 by editing state.json (consec_fail reset to 0) to let it probe past the confirmed 1010-1030 bracket around its current value 1020. Its next_dir is "-", so its next visit will try 1020 - 25 = 995.
- Next constant up (round-robin): `BOOST_DIST_MIN`.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 173)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact reproduction of run 149.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `BOOST_ANGLE_MAX_DEG`.
- `plateaued: true` for a twenty-seventh consecutive cycle.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 172)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact reproduction of run 148.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `BRAKING_THRUST_CAP` — expect another large cliff-edge regression (runs 137, 149, 161).
- `plateaued: true` for a twenty-sixth consecutive cycle. The search has now gone through more than 3.5 full round-robin passes with zero acceptances since run 135, all reproducing prior results almost exactly. Convergence conclusion stands; continuing per standing instruction to keep processing each firing.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 171)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact reproduction of run 147.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `BRAKING_ZONE_SPEED_MULT`.
- Correction to prior note: `BRAKING_ZONE_MIN`'s step size does NOT appear to be shrinking between visits (it retried the exact same ±10 nudges as runs 147/159) — the compass-search implementation seems to just alternate direction without decay once both directions have been rejected at least once, rather than halving the step. Worth keeping in mind: this constant will likely keep oscillating between 1010/1030 tests indefinitely without finding anything finer, since it never probes closer to 1020.
- `plateaued: true` for a twenty-fifth consecutive cycle.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 170)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact reproduction of run 146.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `BRAKING_ZONE_MIN` — the one constant with real, non-cliff structure left (optimum at 1020, bracketed both sides at 1010/1030). Its step size should have shrunk further by now per the compass-search rule; worth watching whether a finer step finds anything between 1010-1030.
- `plateaued: true` for a twenty-fourth consecutive cycle.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 169)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact reproduction of run 145.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `THRUST_ANGLE_FACTOR`.
- `plateaued: true` for a twenty-third consecutive cycle. We are now most of the way through a third full round-robin pass reproducing the first two almost perfectly. This is about as strong a convergence signal as this search methodology can produce.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 168)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact reproduction of run 144.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `THRUST_ZERO_ANGLE_DEG`.
- `plateaued: true` for a twenty-second consecutive cycle.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 167)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact reproduction of run 143.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `DRIFT_CORRECTION_FACTOR` — the sharpest local optimum in the set (both ±0.01 steps cause real damage). Expect another clear regression.
- `plateaued: true` for a twenty-first consecutive cycle.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 166)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact reproduction of run 142.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `TARGET_BLEND_DIST_NORM`.
- `plateaued: true` for a twentieth consecutive cycle. We are now most of the way through a third full pass of the round-robin producing identical results to passes one and two. Reconfirming the earlier conclusion: the search is fully converged at current step sizes.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 165)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact reproduction of run 141.

## What failed
- Nothing new — known Long Straight boost-skip at 792.

## Remember next run
- Next constant up (round-robin): `TARGET_BLEND_MAX_WEIGHT`.
- `plateaued: true` for a nineteenth consecutive cycle.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 164)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact reproduction of run 140.

## What failed
- Nothing new — known Long Straight boost-effectiveness regression at 864.

## Remember next run
- Next constant up (round-robin): `SHIELD_PREDICTED_DIST_TRIGGER`.
- `plateaued: true` for an eighteenth consecutive cycle.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 163)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact 16/16 tie, third confirmation for this constant (runs 139, 151, 163).

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `SHIELD_DIST_TRIGGER` — expect the known Long Straight boost-skip regression again (runs 128, 152).
- `plateaued: true` for a seventeenth consecutive cycle.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 162)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact 16/16 tie, third confirmation of this constant's convergence (runs 138, 150, 162).

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `BOOST_DIST_MIN`.
- `plateaued: true` for a sixteenth consecutive cycle.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 161)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Byte-identical reproduction of run 137's regression.

## What failed
- `BRAKING_THRUST_CAP` step `-` (40 → 39) reproduced its known cliff-edge regression exactly.

## Remember next run
- Next constant up (round-robin): `BOOST_ANGLE_MAX_DEG`.
- `plateaued: true` for a fifteenth consecutive cycle.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 160)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact 16/16 tie, third time this exact nudge has been tried (runs 136, 148, 160) with identical results each time.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `BRAKING_THRUST_CAP` — the most fragile constant in the set (cliff on both sides, runs 137 and 149). Expect another large regression when its turn comes, which will not change the settled conclusion.
- `plateaued: true` for a fourteenth consecutive cycle. Search fully converged; continuing to note this each cycle but no new information is emerging.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 159)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Near-tie rejection, negligible cost difference.

## What failed
- `BRAKING_ZONE_MIN` step `-` (1020 → 1010) had negligible effect (1-turn boost-effectiveness difference on Simple Oval).

## Remember next run
- Next constant up (round-robin): `BRAKING_ZONE_SPEED_MULT`.
- `BRAKING_ZONE_MIN` now bracketed on both sides (1010 this run, 1030 at run 147) — 1020 is confirmed the local optimum. This constant, the last "alive" one, is now also settled. Every constant in the set has now been bracketed or tied on both sides.
- `plateaued: true` for a thirteenth consecutive cycle. With every constant now confirmed settled (converged or cliff-bracketed), the search has no further degrees of freedom left at current step sizes — this is essentially a firm conclusion, not just a plateau. Recommend the user either accept 807 as final for this session or explicitly widen step sizes in `state.json` to explore a coarser resolution.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 158)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Reproduces run 134 essentially exactly.

## What failed
- `THRUST_ANGLE_FACTOR` step `-` (0.7 → 0.693) repeated the same mixed-signal rejection as run 134.

## Remember next run
- Next constant up (round-robin): `BRAKING_ZONE_MIN` — this is the one constant that has produced a real win this session (run 135, 1010→1020) and was also tested at 1020→1030 (run 147, rejected, near-tie). Worth watching whether the shrunk step size finds another small win around 1020-1030, since this is the most "alive" constant left in the set.
- `plateaued: true` for a twelfth consecutive cycle.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 157)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact 16/16 tie, identical to run 133.

## What failed
- Nothing new.

## Remember next run
- Next constant up (round-robin): `THRUST_ANGLE_FACTOR`.
- `plateaued: true` for an eleventh consecutive cycle.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 156)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Same shape as run 132, rejected correctly again.

## What failed
- `DRIFT_CORRECTION_FACTOR` step `+` (1.0 → 1.01) again caused a Long Straight boost-skip. Interesting note: unlike runs 127-134 vs 150-155 which reproduced almost byte-identically, this run's Simple Oval regression was notably milder (189 turns vs 232 at run 132) — suggests some run-to-run variance exists in the harness/candidate build for this particular constant, though the overall accept/reject verdict was unaffected.

## Remember next run
- Next constant up (round-robin): `THRUST_ZERO_ANGLE_DEG`.
- `plateaued: true` for a tenth consecutive cycle.
- Minor observation: not every repeat run is perfectly deterministic (see above) — worth keeping in mind if a close call ever flips between passes, though nothing here has been close.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 155)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Reproduces run 131 almost exactly.

## What failed
- `TARGET_BLEND_DIST_NORM` step `-` (3030 → 3000) repeated the same regression as run 131.

## Remember next run
- Next constant up (round-robin): `DRIFT_CORRECTION_FACTOR`.
- `plateaued: true` for a ninth consecutive cycle. Convergence remains thoroughly confirmed by repeated identical/near-identical results across the second full pass.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 154)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Same mixed-signal shape as run 130, cost function correctly rejected again.

## What failed
- `TARGET_BLEND_MAX_WEIGHT` step `-` (0.22 → 0.218) repeated the same mixed-signal regression as run 130.

## Remember next run
- Next constant up (round-robin): `TARGET_BLEND_DIST_NORM`.
- `plateaued: true` for an eighth consecutive cycle. This second pass continues to mirror the first pass almost exactly (small numeric differences, e.g. Hairpin boost effectiveness -2 vs -3 here, but same overall verdict). Convergence is thoroughly confirmed.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 153)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact 16/16 tie, identical to run 129.

## What failed
- Nothing new — reproduces the known tie.

## Remember next run
- Next constant up (round-robin): `TARGET_BLEND_MAX_WEIGHT`.
- `plateaued: true` for a seventh consecutive cycle. This second pass through the round-robin (runs 150-153 so far) continues to exactly mirror the first pass (126-129). Convergence at current step sizes is now very well established — flagged repeatedly to the user; awaiting their call on whether to keep cycling as-is, widen steps, or stop.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 152)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Identical result to run 128 (same constant, same nudge, same Long Straight boost-skip failure).

## What failed
- `SHIELD_DIST_TRIGGER` step `-` (855 → 846) reproduced the exact same Long Straight regression seen at run 128 — confirms this is a deterministic, reproducible cliff-edge, not simulator noise.

## Remember next run
- Next constant up (round-robin): `SHIELD_PREDICTED_DIST_TRIGGER`.
- `plateaued: true` for a sixth consecutive cycle — no acceptance since run 135 (17 runs ago). We are now in a second full pass through the round-robin producing byte-identical outcomes to the first pass (runs 127-134 mirrored by 151-152 so far). This strongly confirms convergence at current step sizes — further runs are very unlikely to find new wins without a deliberate step-size widen.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 151)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact 16/16 tie, as expected.

## What failed
- `BOOST_DIST_MIN` step `+` (4000 → 4040) had zero effect — identical result to run 127 (same nudge, same tie).

## Remember next run
- Next constant up (round-robin): `SHIELD_DIST_TRIGGER`.
- `plateaued: true` for a fifth consecutive cycle — no acceptance since run 135. The search is now essentially just re-confirming fully-converged constants each cycle. Strongly recommend treating 807 as the settled result for this session unless the user wants to widen step sizes to keep probing.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 150)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact 16/16 tie, as expected for this fully-converged constant.

## What failed
- `BOOST_ANGLE_MAX_DEG` step `+` (10 → 11) had zero effect — same as run 126, this constant's `+` direction has now tied exactly twice.

## Remember next run
- Next constant up (round-robin): `BOOST_DIST_MIN`.
- `plateaued: true` for a fourth consecutive cycle — no acceptance since run 135. The search has now cycled through every constant multiple times since the last win, mostly reconfirming known-converged or known-cliff values. This is a strong signal the search has converged at current step sizes; the user may want to consider widening a step to keep probing, or treating 807 as the settled result for this session.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 149)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Worst-magnitude regression of the whole session, caught cleanly.

## What failed
- `BRAKING_THRUST_CAP` step `+` (40 → 41) was even worse than the `-` regression at run 137: Simple Oval blew up to 308 turns (from 184) and Long Straight blew up to 300 turns with a full boost-skip. Net cost +301, the largest single-run regression seen this session.

## Remember next run
- Next constant up (round-robin): `BOOST_ANGLE_MAX_DEG`.
- `BRAKING_THRUST_CAP` at 40 is now confirmed a very sharp cliff-edge optimum on BOTH sides (39 bad at run 137, 41 much worse here). This is the most fragile constant in the whole set — do not widen its step size if ever considering manual intervention.
- `plateaued: true` for a third consecutive cycle — no acceptance since run 135. Every constant that hits a cliff or converges keeps reconfirming; genuinely running low on remaining improvements at current step sizes. Worth surfacing to the user as a natural stopping point or a signal to consider widening steps.
- Long Straight fragility watch: another full boost-skip (300 turns) — this time clearly tied to the `BRAKING_THRUST_CAP` cliff, not a SHIELD-related cause like earlier occurrences. Reinforces that Long Straight's boost logic is sensitive to multiple different constants near their edges.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 148)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Clear, severe regression caught correctly.

## What failed
- `BRAKING_ZONE_SPEED_MULT` step `+` (3.0 → 3.03) caused a sharp Simple Oval blowup (184 → 287 turns), same failure shape as run 137's `BRAKING_THRUST_CAP` regression (late boost, negative effectiveness). Net cost +178.

## Remember next run
- Next constant up (round-robin): `BRAKING_THRUST_CAP`.
- `BRAKING_ZONE_SPEED_MULT` at 3.0 is a real local optimum, not just a flat plateau — `+` direction breaks Simple Oval badly. Recall `-` direction (2.97) tied exactly at run 136, so this constant has one flat side and one cliff side.
- `plateaued: true` again this run — still no acceptance since run 135 (`BRAKING_ZONE_MIN`). Every other constant this cycle has been solidly re-confirmed at its current value; may be worth treating the search as converged soon if no more wins appear, though nothing wrong is happening.
- Long Straight fragility watch: no issue this run. Last full boost-skip was run 141.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 147)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Near-tie rejection.

## What failed
- `BRAKING_ZONE_MIN` step `+` (1020 → 1030) had negligible effect (2-cost regression on Mixed only). Rejected on a technicality.

## Remember next run
- Next constant up (round-robin): `BRAKING_ZONE_SPEED_MULT`.
- **Plateaued: true this run** — a full round-robin cycle completed with zero acceptances (last acceptance was run 135, `BRAKING_ZONE_MIN` 1010→1020). Per the skill's guidance: treat the search as converged at current step sizes, or manually widen a step in `state.json` to keep probing. Worth flagging to the user next time they're active.
- `BRAKING_ZONE_MIN` now bracketed at both 1010 (old) and 1030 (this run) as worse than 1020 — 1020 looks like a solid local optimum.
- Long Straight fragility watch: no issue this run. Last full boost-skip was run 141.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 146)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Decisive, unambiguous result: A won every non-tied metric (6/16), B won none.

## What failed
- `THRUST_ANGLE_FACTOR` step `+` (0.7 → 0.707) regressed all 3 non-Mixed tracks on both turns and boost effectiveness. Combined with run 134's `-` direction also rejected, this constant is now bracketed on both sides at 0.7.

## Remember next run
- Next constant up (round-robin): `BRAKING_ZONE_MIN`.
- `THRUST_ANGLE_FACTOR` fully settled at 0.7 — both directions tested and clearly rejected.
- Long Straight fragility watch: boost effectiveness dropped again (13 vs 86) alongside a turn-count regression (250 vs 240) — a combined/worse variant of the usual pattern, but still no full boost-skip (boost did fire, just later/less effective). Last full skip was run 141.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).
- `BRAKING_ZONE_MIN` (next up) was last accepted at run 135 (1010→1020); per that run's note, worth watching for another `+` win here given the last move in this direction succeeded.

---

# tune-bot run — 2026-07-05 (run 145)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Near-total tie, tiny rejection (+1 cost).

## What failed
- `THRUST_ZERO_ANGLE_DEG` step `+` (91 → 92) had negligible effect (1-turn boost-effectiveness difference on Hairpin only). Rejected on a technicality, not a real regression.

## Remember next run
- Next constant up (round-robin): `THRUST_ANGLE_FACTOR`.
- `THRUST_ZERO_ANGLE_DEG` remains settled at 91; step size should keep shrinking per compass-search rule since this was a near-tie, not a hard rejection.
- Long Straight fragility watch: no issue this run. Last full boost-skip was run 141.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 144)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Clear rejection, dominated by a severe Simple Oval regression.

## What failed
- `DRIFT_CORRECTION_FACTOR` step `-` (1.0 → 0.99) caused Simple Oval to blow up (184 → 225 turns, boost effectiveness went negative). Combined with run 132's `+` direction (1.01) also causing a severe regression (there on Simple Oval AND Long Straight), this constant is now confirmed a very sharp local optimum — both directions break things badly even at tiny (0.01) steps.

## Remember next run
- Next constant up (round-robin): `THRUST_ZERO_ANGLE_DEG`.
- `DRIFT_CORRECTION_FACTOR` at 1.0 is a hard local optimum, sharper than most other converged constants — both ±0.01 steps cause real damage (not just ties). Treat as fully settled; further probing here is unlikely to help.
- Long Straight fragility watch: mild version again (241 turns, boost effectiveness down but still positive) — no full boost-skip. Last full skip was run 141.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 143)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Small turn-count gains on 2 tracks were again correctly outweighed by boost-effectiveness losses (same pattern as run 130).

## What failed
- `TARGET_BLEND_DIST_NORM` step `+` (3030 → 3060) regressed boost effectiveness on 3 of 4 tracks despite tiny turn-count wins on 2. Combined with run 131's `-` direction also rejected, this constant is bracketed on both sides at 3030.

## Remember next run
- Next constant up (round-robin): `DRIFT_CORRECTION_FACTOR`.
- `TARGET_BLEND_DIST_NORM` fully settled at 3030 — both directions tested and rejected.
- Long Straight fragility watch: no full boost-skip this run, but boost effectiveness was notably worse for B (30 vs 86) — the milder variant again. Last full boost-skip was run 141.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 142)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Clear result — A won decisively (7/16 vs 1/16), no ambiguity.

## What failed
- `TARGET_BLEND_MAX_WEIGHT` step `+` (0.22 → 0.222) regressed 3 of 4 tracks on turns and boost effectiveness. Combined with run 130's `-` direction also being rejected, this constant is now bracketed on both sides at 0.22.

## Remember next run
- Next constant up (round-robin): `TARGET_BLEND_DIST_NORM`.
- `TARGET_BLEND_MAX_WEIGHT` fully settled at 0.22 — both directions tested and rejected.
- Long Straight fragility watch: no full boost-skip this run, but boost effectiveness was much worse for B (9 vs 86) — another instance of the milder variant. Last full boost-skip was run 141.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 141)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Clean single-track regression, no ambiguity.

## What failed
- `SHIELD_PREDICTED_DIST_TRIGGER` step `-` (800 → 792) caused another full Long Straight boost-skip (240 → 246 turns, boost never fired) — this is now the classic full-blown version of the fragility pattern (as opposed to run 140's milder "boost fired but less effective" variant).

## Remember next run
- Next constant up (round-robin): `TARGET_BLEND_MAX_WEIGHT`.
- `SHIELD_PREDICTED_DIST_TRIGGER` at 800 (current accepted best) rejected on the `-` side now; `+` side was already tied (run 129). This constant is effectively settled at 800.
- Long Straight fragility watch: two SHIELD-related constants in a row (`SHIELD_DIST_TRIGGER` run 140, `SHIELD_PREDICTED_DIST_TRIGGER` run 141) have now triggered Long Straight boost issues when nudged away from their accepted values. This suggests the SHIELD trigger logic and the boost trigger logic on this track interact — still just an observation, not investigated.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 140)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Clean single-track regression, no ambiguity.

## What failed
- `SHIELD_DIST_TRIGGER` step `+` (855 → 864) dropped Long Straight's boost effectiveness sharply (86 → 17), though it still finished on time at 240 turns (no DNF, unlike some past Long Straight incidents). Net cost +69.

## Remember next run
- Next constant up (round-robin): `SHIELD_PREDICTED_DIST_TRIGGER`.
- `SHIELD_DIST_TRIGGER` at 855 (current accepted best) looks like a local optimum on both sides now: `-` direction (846) regressed badly at run 128, `+` direction (864) regresses more mildly here. Bracketed from both sides — solidly settled at 855.
- Long Straight fragility watch: another mild instance (boost effectiveness down, not a full DNF/boost-skip). Pattern keeps concentrating on this track's boost trigger sensitivity specifically — still just an observation.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 139)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact 16/16 tie; both directions of `BOOST_DIST_MIN` now tested (recall run 127 tried `+`, also tied) with no effect either way — genuinely converged, same pattern as `BOOST_ANGLE_MAX_DEG`.

## What failed
- `BOOST_DIST_MIN` step `-` (4000 → 3960) had zero measurable effect on any track.

## Remember next run
- Next constant up (round-robin): `SHIELD_DIST_TRIGGER`.
- `BOOST_DIST_MIN` genuinely converged/flat at 4000 (both directions tied exactly across separate runs).
- Long Straight fragility watch: no issue this run. Last actual occurrence was run 137.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 138)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact 16/16 tie, both directions of `BOOST_ANGLE_MAX_DEG` now tested with no effect.

## What failed
- `BOOST_ANGLE_MAX_DEG` step `-` (10 → 9) had zero measurable effect on any track.

## Remember next run
- Next constant up (round-robin): `BOOST_DIST_MIN`.
- `BOOST_ANGLE_MAX_DEG` genuinely converged/flat at 10 (both `+` and `-` directions tied exactly across separate runs) — this one really is flat, unlike `BRAKING_THRUST_CAP` which only looked converged until a cliff-edge regression appeared in run 137. Worth keeping that distinction in mind: a tie in both directions is stronger evidence of convergence than a tie in just one direction.
- Long Straight fragility watch: no issue this run. Last actual occurrence was run 137 (different flavor — see that run's notes).
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 137)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Harness clearly caught a severe regression — no ambiguity in the reject decision.

## What failed
- `BRAKING_THRUST_CAP` step `-` (40 → 39) caused a sharp cliff-edge regression: Simple Oval blew up from 184 to 287 turns (boosted 140 turns later than before, saving only 5 turns instead of 47), and Long Straight regressed from 240 to 269 turns with boost effectiveness going negative (86 → -25). Net cost +220, by far the worst regression seen in a while. Notable because this constant had been informally labeled "fully converged" in earlier session notes — this result shows that label was premature; there's a sharp edge just below 40, not a flat plateau.

## Remember next run
- Next constant up (round-robin): `BOOST_ANGLE_MAX_DEG`.
- **Correction to prior notes:** `BRAKING_THRUST_CAP` is NOT flat/converged — 40 is right at a cliff edge on the low side. Do not assume small steps are safe here; if `+` direction gets tried next, watch for asymmetric behavior (may be fine above 40 but breaks below it).
- Long Straight fragility watch: this run's Long Straight failure looks mechanistically different from the usual "skips boost" pattern — here boost still fired but late/negative-effect, alongside a braking-cap-driven overall slowdown. Worth distinguishing from the historical pattern when reviewing later.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 136)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact 16/16 tie.

## What failed
- `BRAKING_ZONE_SPEED_MULT` step `-` (3.0 → 2.97) had zero measurable effect on any track — consistent with prior rejections/ties for this constant.

## Remember next run
- Next constant up (round-robin): `BRAKING_THRUST_CAP`.
- `BRAKING_ZONE_SPEED_MULT` continues to look converged/flat around 3.0.
- Long Straight fragility watch: no issue this run. Last actual occurrence was run 132.
- Current best cost holds at 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 135)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- `BRAKING_ZONE_MIN` step `+` (1010 → 1020) found a genuine small win: Simple Oval boost effectiveness improved by 1 turn saved, everything else tied. Cost 808 → 807. Committed cleanly as `0d72f5e`, single-line diff confirmed via `git diff --stat` before staging.

## What failed
- Nothing this run.

## Remember next run
- Next constant up (round-robin): `BRAKING_ZONE_SPEED_MULT`.
- `BRAKING_ZONE_MIN` now at 1020 (accepted); worth trying `+` again next time this constant comes up in the round-robin, since the last move in this direction just won.
- Long Straight fragility watch: no issue this run (clean tie). Last actual occurrence was run 132.
- Current best cost now 807 (7 accepted commits total, ~8.2% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 134)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Mixed-signal case handled correctly again: no track had a catastrophic failure, but the aggregate across small wins/losses on 4 tracks came out clearly worse for the candidate.

## What failed
- `THRUST_ANGLE_FACTOR` step `-` (0.7 → 0.693) was a wash overall: small win on Simple Oval, but Hairpin regressed (255→265 turns) and Long Straight's boost effectiveness dropped sharply (86→24, though it still finished in 240 turns — no DNF, no dropped boost, just less benefit from it). Net cost +75.

## Remember next run
- Next constant up (round-robin): `BRAKING_ZONE_MIN`.
- `THRUST_ANGLE_FACTOR` at 0.7 continues to look like the right value; `-` direction rejected, step size should shrink per compass-search rule before trying `-` again (or moving to `+`).
- Long Straight fragility watch: this run it didn't DNF or skip boost, just got less benefit from it (24 vs 86 turns saved) — a milder version of the usual pattern, still on the same track. No action needed.
- Current best cost holds at 808 (6 accepted commits total, ~8.1% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 133)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Exact 16/16 tie, no Long Straight instability this run.

## What failed
- `THRUST_ZERO_ANGLE_DEG` step `-` (91 → 90) had zero effect on any track. This constant has now been tied/rejected in both directions around 91 multiple times — solidly converged.

## Remember next run
- Next constant up (round-robin): `THRUST_ANGLE_FACTOR`.
- `THRUST_ZERO_ANGLE_DEG` fully converged at 91.
- Long Straight fragility watch: no issue this run. Last actual occurrence was run 132.
- Current best cost holds at 808 (6 accepted commits total, ~8.1% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 132)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Scalarized cost correctly rejected a candidate that "won" more raw metrics (4/15 vs 3/15) because the magnitude of its two losses (Simple Oval +48 turns, Long Straight +24 turns and lost boost) dwarfed its small gains elsewhere. Good evidence the cost function, not a naive win-count, is doing the right thing here.

## What failed
- `DRIFT_CORRECTION_FACTOR` step `+` (1.0 → 1.01) caused a severe Simple Oval regression (184→232 turns, boost effectiveness 46→4) and another Long Straight fragility occurrence (240→264 turns, boost never fired — now ~10 occurrences total across the run history).

## Remember next run
- Next constant up (round-robin): `THRUST_ZERO_ANGLE_DEG`.
- `DRIFT_CORRECTION_FACTOR` at 1.0 is clearly a strong local optimum on the `+` side — a step of only 0.01 caused a large regression on 2 of 4 tracks. This constant's step size should keep shrinking per the compass-search rule.
- Long Straight fragility watch: another occurrence this run (boost unused, ~10th total). Still not manually investigated — worth flagging to the user again if this keeps recurring at this rate.
- Current best cost holds at 808 (6 accepted commits total, ~8.1% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 131)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Clear, consistent result this time — A (current best) won or tied on every metric across all 4 tracks, no ambiguity.

## What failed
- `TARGET_BLEND_DIST_NORM` step `-` (3030 → 3000) was clearly worse: Simple Oval and Mixed both regressed on turns and boost effectiveness. Net cost +24.

## Remember next run
- Next constant up (round-robin): `DRIFT_CORRECTION_FACTOR`.
- `TARGET_BLEND_DIST_NORM` at 3030 (current accepted best) continues to look like a solid local optimum — both directions now tested worse (recall run ~ earlier `+` direction was also rejected per state.json history).
- Long Straight fragility watch: no issue this run (clean 240 turns both sides). Still open for a future manual look; last actual regression was run 128.
- Current best cost holds at 808 (6 accepted commits total, ~8.1% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 130)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. Harness correctly caught a mixed-signal case: raw turn counts were mostly flat-to-slightly-better for B, but the cost function properly penalized a real regression (boost effectiveness collapsing on 3/4 tracks) that a naive "turns-to-finish only" comparison would have missed.

## What failed
- `TARGET_BLEND_MAX_WEIGHT` step `-` (0.22 → 0.218) shaved 1-2 turns off 3 tracks but tanked boost effectiveness badly on Simple Oval (46→8) and Long Straight (86→16), and worsened it further on Hairpin (-3→-29). Net cost +130, clearly worse overall despite looking like a slight win on raw turns.

## Remember next run
- Next constant up (round-robin): `TARGET_BLEND_DIST_NORM`.
- `TARGET_BLEND_MAX_WEIGHT` at 0.22 (current accepted best) is a good local optimum — a step of just 0.002 down is enough to break boost timing on multiple tracks. Confirms this constant is sensitive; don't widen its step size.
- Long Straight fragility watch: no DNF this run, but did show reduced boost effectiveness alongside Simple Oval/Hairpin — this may be the same underlying mechanism as the historical DNF pattern (boost-trigger logic reacting to TARGET_BLEND changes), worth keeping in mind if a manual investigation ever happens.
- Current best cost holds at 808 (6 accepted commits total, ~8.1% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 129)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline clean. All 4 tracks tied exactly (16/16) — no Long Straight instability this time, unlike run 128.

## What failed
- `SHIELD_PREDICTED_DIST_TRIGGER` step `+` (800 → 808) had zero effect on any track; consistent with this constant being fully converged at current step size.

## Remember next run
- Next constant up (round-robin): `TARGET_BLEND_MAX_WEIGHT`.
- `SHIELD_PREDICTED_DIST_TRIGGER` looks converged/flat around 800 for now.
- Long Straight fragility watch: no issue this run (clean 240 turns both sides, boost fired normally). Still an open item for a manual look eventually — count stands at ~9 prior occurrences (run 128 most recent).
- Current best cost holds at 808 (6 accepted commits total, ~8.1% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 128)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Full pipeline (sanity build, tune_step.py, verify build, logs) completed cleanly.
- 3 of 4 tracks tied exactly; the search correctly rejected on the fourth track's regression alone.

## What failed
- `SHIELD_DIST_TRIGGER` step `-` (855 → 846) regressed Long Straight badly: 240 → 279 turns, and the pod never boosted at all on that track (vs. boosting and saving 86 turns at the old value). This is yet another **Long Straight fragility** occurrence — now at least 9 distinct times across many different constants/directions. The pattern: a small nudge to almost any constant occasionally causes this one track to skip its boost entirely and lose ~30-40 turns. Still not manually investigated; still just an observation, no explicit instruction to dig in yet.

## Remember next run
- Next constant up (round-robin): `SHIELD_PREDICTED_DIST_TRIGGER`.
- `SHIELD_DIST_TRIGGER` seems to have a sharp edge just below 855 (846 is notably worse, not just noisy) — the current step size for this constant may need to shrink further before probing `-` again; state.json will handle that automatically via the shrink-on-reject rule.
- Long Straight fragility watch: recommend a manual look eventually — worth checking whether the boost-trigger logic has a discontinuity near default track geometry on this route specifically.
- Current best cost holds at 808 (6 accepted commits total, ~8.1% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 127)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none

## What worked
- Sanity build, tune_step.py run, verify build, and log writes all completed cleanly in one pass.
- All 4 tracks tied exactly (16/16) for `BOOST_DIST_MIN` +40 step — no DNFs anywhere this run, including Long Straight (which has been the flaky one historically).

## What failed
- `BOOST_DIST_MIN` at step +40 (4000 → 4040) produced an exact tie, same as several prior nudges to this constant — it's within a previously-noted converged/plateaued range.

## Remember next run
- `BOOST_DIST_MIN` is essentially flat around 4000 at this step size; likely fully converged like `BOOST_ANGLE_MAX_DEG` and `BRAKING_THRUST_CAP`.
- Next constant up (round-robin): `SHIELD_DIST_TRIGGER`.
- Long Straight fragility watch: no issue this run — track finished clean at 240 turns both sides, no DNF/checkpoint-miss. Still worth a manual look at some point given past occurrences, but not urgent.
- Current best cost holds at 808 (6 accepted commits total so far, ~8.1% improvement over baseline 879).

---

# tune-bot run — 2026-07-05 (run 126)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BOOST_ANGLE_MAX_DEG` confirming pass-through — exact 16/16 tie, as expected.

## What failed
- Nothing failed; nudge rejected (expected outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 91 (accepted, committed `aaa0454`) — owes `-0.5625` (91→90.4375) at halved step next.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN` = 1010 (accepted, committed `1dae7db`) — owes `+0.390625` (1010→1010.390625) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: owes `-0.03` (3.0→2.97) at halved step next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: nine exact ties — completely converged — next constant up, expect another quick pass-through.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-0.3515625` (855→854.6484375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+2` (800→802) at halved step next.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — confirmed cliff on `+` side. Owes `-0.00015625` (0.22→0.21984375) next.
- `TARGET_BLEND_DIST_NORM` = 3030 (accepted, committed `fab80fd`) — `+` side has a cliff nearby too. Owes `-18.75` (3030→3011.25) at halved step next.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0. Owes `+0.005` (1.0→1.005) at halved step next.
- **Current best cost: 808**. Six acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`), run 107 (`fab80fd`), run 121 (`aaa0454`), run 123 (`1dae7db`). Streak: 3 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 125)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BRAKING_THRUST_CAP` +1 rejected (1106 vs 808) — Simple Oval and Long Straight blew up despite Hairpin/Mixed wins. 40 confirmed.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 91 (accepted, committed `aaa0454`) — owes `-0.5625` (91→90.4375) at halved step next.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN` = 1010 (accepted, committed `1dae7db`) — owes `+0.390625` (1010→1010.390625) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: owes `-0.03` (3.0→2.97) at halved step next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor — next constant up, expect quick pass-through.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: nine exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-0.3515625` (855→854.6484375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+2` (800→802) at halved step next.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — confirmed cliff on `+` side. Owes `-0.00015625` (0.22→0.21984375) next.
- `TARGET_BLEND_DIST_NORM` = 3030 (accepted, committed `fab80fd`) — `+` side has a cliff nearby too. Owes `-18.75` (3030→3011.25) at halved step next.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0. Owes `+0.005` (1.0→1.005) at halved step next.
- **Current best cost: 808**. Six acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`), run 107 (`fab80fd`), run 121 (`aaa0454`), run 123 (`1dae7db`). Streak: 2 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 124)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BRAKING_ZONE_SPEED_MULT` +0.03 rejected hard against the new 808 baseline (983 vs 808) — Simple Oval devastated again.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 91 (accepted, committed `aaa0454`) — owes `-0.5625` (91→90.4375) at halved step next.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN` = 1010 (accepted, committed `1dae7db`) — owes `+0.390625` (1010→1010.390625) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: owes `-0.03` (3.0→2.97) at halved step next — next constant up.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: nine exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-0.3515625` (855→854.6484375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+2` (800→802) at halved step next.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — confirmed cliff on `+` side. Owes `-0.00015625` (0.22→0.21984375) next.
- `TARGET_BLEND_DIST_NORM` = 3030 (accepted, committed `fab80fd`) — `+` side has a cliff nearby too. Owes `-18.75` (3030→3011.25) at halved step next.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0. Owes `+0.005` (1.0→1.005) at halved step next.
- **Current best cost: 808**. Six acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`), run 107 (`fab80fd`), run 121 (`aaa0454`), run 123 (`1dae7db`). Streak: 1 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 123) — SIXTH ACCEPTANCE

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- **Sixth win**: `BRAKING_ZONE_MIN` 1000→1010 dropped cost 814→808. This is the constant with the most persistent history of razor-thin, single-digit-point rejections across the whole search (832, 833, 830, 837, 832, 817) — it finally crossed over at run 123. Great validation that the compass search's patience with tight-margin constants pays off eventually rather than being a waste of cycles. Committed as `1dae7db`.

## What failed
- Nothing failed.

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 91 (accepted, committed `aaa0454`) — owes `-0.5625` (91→90.4375) at halved step next.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN` = **1010 (accepted, committed `1dae7db`)**. Owes `+0.390625` (1010→1010.390625) at halved step next (accepting direction keeps its `next_dir`) — recompare against new 808 baseline.
- `BRAKING_ZONE_SPEED_MULT`: owes `+0.015` (3.0→3.015) at halved step next — next constant up.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: nine exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-0.3515625` (855→854.6484375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+2` (800→802) at halved step next.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — confirmed cliff on `+` side. Owes `-0.00015625` (0.22→0.21984375) next.
- `TARGET_BLEND_DIST_NORM` = 3030 (accepted, committed `fab80fd`) — `+` side has a cliff nearby too. Owes `-18.75` (3030→3011.25) at halved step next.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0. Owes `+0.005` (1.0→1.005) at halved step next.
- **Current best cost: 808**. Six acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`), run 107 (`fab80fd`), run 121 (`aaa0454`), run 123 (`1dae7db`). Streak reset to 0.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 122)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `THRUST_ANGLE_FACTOR` +0.007 rejected against the new 814 baseline, clean sweep (927 vs 814). Consistent with established pattern.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 91 (accepted, committed `aaa0454`) — owes `-0.5625` (91→90.4375) at halved step next.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: needs re-comparison against new 814 baseline. Owes `+0.390625` (1000→1000.390625) at halved step next — next constant up.
- `BRAKING_ZONE_SPEED_MULT`: owes `+0.015` (3.0→3.015) at halved step next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: nine exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-0.3515625` (855→854.6484375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+2` (800→802) at halved step next.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — confirmed cliff on `+` side. Owes `-0.00015625` (0.22→0.21984375) next.
- `TARGET_BLEND_DIST_NORM` = 3030 (accepted, committed `fab80fd`) — `+` side has a cliff nearby too. Owes `-18.75` (3030→3011.25) at halved step next.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0. Owes `+0.005` (1.0→1.005) at halved step next.
- **Current best cost: 814**. Five acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`), run 107 (`fab80fd`), run 121 (`aaa0454`). Streak: 1 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 121) — FIFTH ACCEPTANCE

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- **Fifth win**: `THRUST_ZERO_ANGLE_DEG` 92→91 dropped cost 816→814 — Hairpin's boost effectiveness improved (-8→-6) with zero losses. Committed as `aaa0454`.
- Interesting: this constant had rejected both `+1.125` and `-1.125` nudges (runs 73, 97) by exactly 1 point each, and I'd flagged it as "very likely fully converged." A smaller `-1` step this run proves that wrong — worth remembering that even razor-thin repeated rejections don't guarantee true convergence; the search keeps shrinking steps for a reason.

## What failed
- Nothing failed.

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = **91 (accepted, committed `aaa0454`)**. Owes `-0.5625` (91→90.4375) at halved step next (accepting direction keeps its `next_dir`, step wasn't halved on acceptance).
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step — next constant up, recompare against new 814 baseline.
- `BRAKING_ZONE_MIN`: needs re-comparison against new baseline. Owes `+0.390625` (1000→1000.390625) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: owes `+0.015` (3.0→3.015) at halved step next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: nine exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-0.3515625` (855→854.6484375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+2` (800→802) at halved step next.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — confirmed cliff on `+` side. Owes `-0.00015625` (0.22→0.21984375) next.
- `TARGET_BLEND_DIST_NORM` = 3030 (accepted, committed `fab80fd`) — `+` side has a cliff nearby too. Owes `-18.75` (3030→3011.25) at halved step next.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0. Owes `+0.005` (1.0→1.005) at halved step next.
- **Current best cost: 814**. Five acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`), run 107 (`fab80fd`), run 121 (`aaa0454`). Streak reset to 0.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 120)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `DRIFT_CORRECTION_FACTOR` -0.01 rejected hard (1044 vs 816) — Simple Oval devastated, boost effectiveness flipped from +46 to -41. 1.0 remains confirmed.

## What failed
- Nothing failed; nudge rejected (normal outcome, if severe).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step — next constant up.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: reconfirmed razor-thin against new baseline. Owes `+0.390625` (1000→1000.390625) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: first exact tie found; owes `+0.015` (3.0→3.015) at halved step next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: nine exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-0.3515625` (855→854.6484375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+2` (800→802) at halved step next.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — confirmed cliff on `+` side. Owes `-0.00015625` (0.22→0.21984375) next.
- `TARGET_BLEND_DIST_NORM` = 3030 (accepted, committed `fab80fd`) — `+` side has a cliff nearby too. Owes `-18.75` (3030→3011.25) at halved step next.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, reconfirmed against new baseline too. Owes `+0.005` (1.0→1.005) at halved step next.
- **Current best cost: 816**. Four acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`), run 107 (`fab80fd`). Streak: 13 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 119) — CYCLE CLOSED

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `TARGET_BLEND_DIST_NORM` +30 rejected (902 vs 816) despite B winning more raw metrics — Long Straight's boost effectiveness collapse dominates. Closes a full cycle with only run 107's acceptance since the last boundary.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: reconfirmed razor-thin against new baseline. Owes `+0.390625` (1000→1000.390625) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: first exact tie found; owes `+0.015` (3.0→3.015) at halved step next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: nine exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-0.3515625` (855→854.6484375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+2` (800→802) at halved step next.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — confirmed cliff on `+` side even at very fine resolution. Owes `-0.00015625` (0.22→0.21984375) next.
- `TARGET_BLEND_DIST_NORM` = 3030 (accepted, committed `fab80fd`) — `+` side has a cliff nearby too now. Owes `-18.75` (3030→3011.25) at halved step next.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0. Owes `-0.005` (1.0→0.995) at halved step next — next constant up, new cycle.
- **Current best cost: 816**. Four acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`), run 107 (`fab80fd`). Streak: 12 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 118)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `TARGET_BLEND_MAX_WEIGHT` +0.002 rejected clean sweep (936 vs 816) — 0.22 remains near the edge of a cliff on the `+` side even at fine resolution.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: reconfirmed razor-thin against new baseline. Owes `+0.390625` (1000→1000.390625) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: first exact tie found; owes `+0.015` (3.0→3.015) at halved step next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: nine exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-0.3515625` (855→854.6484375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+2` (800→802) at halved step next.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — confirmed cliff on `+` side even at very fine resolution. Owes `-0.00015625` (0.22→0.21984375) next.
- `TARGET_BLEND_DIST_NORM` = 3030 (accepted, committed `fab80fd`). Owes `+37.5` (3030→3067.5) next — next constant up.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0. Owes `-0.005` (1.0→0.995) at halved step next.
- **Current best cost: 816**. Four acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`), run 107 (`fab80fd`). Streak: 11 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 117)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `SHIELD_PREDICTED_DIST_TRIGGER` -8 rejected (908 vs 816) — Long Straight lost its boost entirely, everything else exact tie. Step halves to 2.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: reconfirmed razor-thin against new baseline. Owes `+0.390625` (1000→1000.390625) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: first exact tie found; owes `+0.015` (3.0→3.015) at halved step next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: nine exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-0.3515625` (855→854.6484375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+2` (800→802) at halved step next.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — at float precision floor. Owes `-0.0003125` (0.22→0.2196875) next — next constant up.
- `TARGET_BLEND_DIST_NORM` = 3030 (accepted, committed `fab80fd`). Owes `+37.5` (3030→3067.5) next.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0. Owes `-0.005` (1.0→0.995) at halved step next.
- **Current best cost: 816**. Four acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`), run 107 (`fab80fd`). Streak: 10 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 116)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `SHIELD_DIST_TRIGGER` +11.25 rejected (885 vs 816) — Long Straight's boost effectiveness dropped, everything else tied. Step halves to 0.3515625.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: reconfirmed razor-thin against new baseline. Owes `+0.390625` (1000→1000.390625) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: first exact tie found; owes `+0.015` (3.0→3.015) at halved step next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: nine exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-0.3515625` (855→854.6484375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: first exact tie found (+8); owes `-4` (800→796) at halved step next — next constant up.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — at float precision floor. Owes `-0.0003125` (0.22→0.2196875) next.
- `TARGET_BLEND_DIST_NORM` = 3030 (accepted, committed `fab80fd`). Owes `+37.5` (3030→3067.5) next.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0. Owes `-0.005` (1.0→0.995) at halved step next.
- **Current best cost: 816**. Four acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`), run 107 (`fab80fd`). Streak: 9 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 115)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BOOST_DIST_MIN` confirming pass-through — ninth exact tie. Completely converged.

## What failed
- Nothing failed; nudge rejected (expected outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: reconfirmed razor-thin against new baseline. Owes `+0.390625` (1000→1000.390625) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: first exact tie found; owes `+0.015` (3.0→3.015) at halved step next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: **nine exact ties now** — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+0.703125` (855→855.703125) at halved step next — next constant up.
- `SHIELD_PREDICTED_DIST_TRIGGER`: first exact tie found (+8); owes `-4` (800→796) at halved step next.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — at float precision floor. Owes `-0.0003125` (0.22→0.2196875) next.
- `TARGET_BLEND_DIST_NORM` = 3030 (accepted, committed `fab80fd`). Owes `+37.5` (3030→3067.5) next.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0. Owes `-0.005` (1.0→0.995) at halved step next.
- **Current best cost: 816**. Four acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`), run 107 (`fab80fd`). Streak: 8 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 114)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BOOST_ANGLE_MAX_DEG` confirming pass-through — exact 16/16 tie, as expected. Even the Hairpin edge-sensitivity noted earlier this search is gone under the current configuration.

## What failed
- Nothing failed; nudge rejected (expected outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: reconfirmed razor-thin against new baseline. Owes `+0.390625` (1000→1000.390625) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: first exact tie found; owes `+0.015` (3.0→3.015) at halved step next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: eight exact ties — completely converged — next constant up, expect another quick pass-through.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+0.703125` (855→855.703125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: first exact tie found (+8); owes `-4` (800→796) at halved step next.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — at float precision floor. Owes `-0.0003125` (0.22→0.2196875) next.
- `TARGET_BLEND_DIST_NORM` = 3030 (accepted, committed `fab80fd`). Owes `+37.5` (3030→3067.5) next.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0. Owes `-0.005` (1.0→0.995) at halved step next.
- **Current best cost: 816**. Four acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`), run 107 (`fab80fd`). Streak: 7 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 113)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BRAKING_THRUST_CAP` -1 rejected (1015 vs 816) — Simple Oval's boost delayed catastrophically despite a striking Hairpin win. 40 remains confirmed.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: reconfirmed razor-thin against new baseline. Owes `+0.390625` (1000→1000.390625) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: first exact tie found; owes `+0.015` (3.0→3.015) at halved step next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right) — next constant up, expect quick pass-through.
- `BOOST_DIST_MIN`: eight exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+0.703125` (855→855.703125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: first exact tie found (+8); owes `-4` (800→796) at halved step next.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — at float precision floor. Owes `-0.0003125` (0.22→0.2196875) next.
- `TARGET_BLEND_DIST_NORM` = 3030 (accepted, committed `fab80fd`). Owes `+37.5` (3030→3067.5) next.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0. Owes `-0.005` (1.0→0.995) at halved step next.
- **Current best cost: 816**. Four acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`), run 107 (`fab80fd`). Streak: 6 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 112)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BRAKING_ZONE_SPEED_MULT` first exact tie at ±0.03 — the step is now small enough to be fully insensitive around 3.0.

## What failed
- Nothing failed; nudge rejected (expected outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: reconfirmed razor-thin against new baseline (817 vs 816). Owes `+0.390625` (1000→1000.390625) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: first exact tie found; owes `+0.015` (3.0→3.015) at halved step next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor — next constant up.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: eight exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+0.703125` (855→855.703125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: first exact tie found (+8); owes `-4` (800→796) at halved step next.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — at float precision floor. Owes `-0.0003125` (0.22→0.2196875) next.
- `TARGET_BLEND_DIST_NORM` = 3030 (accepted, committed `fab80fd`). Owes `+37.5` (3030→3067.5) next.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0. Owes `-0.005` (1.0→0.995) at halved step next.
- **Current best cost: 816**. Four acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`), run 107 (`fab80fd`). Streak: 5 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 111)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BRAKING_ZONE_MIN` -10 rejected by just 1 point (817 vs 816) against the new baseline — continues this constant's pattern of extremely tight margins around 1000.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: reconfirmed razor-thin against new baseline (817 vs 816). Owes `+0.390625` (1000→1000.390625) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.0046875` (3.0→2.9953125) at halved step — next constant up.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: eight exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+0.703125` (855→855.703125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: first exact tie found (+8); owes `-4` (800→796) at halved step next.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — at float precision floor. Owes `-0.0003125` (0.22→0.2196875) next.
- `TARGET_BLEND_DIST_NORM` = 3030 (accepted, committed `fab80fd`). Owes `+37.5` (3030→3067.5) next.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0. Owes `-0.005` (1.0→0.995) at halved step next.
- **Current best cost: 816**. Four acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`), run 107 (`fab80fd`). Streak: 4 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 110)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `THRUST_ANGLE_FACTOR` -0.007 rejected (923 vs 816) — Long Straight's boost effectiveness collapsed (86→24), dominating despite small wins elsewhere.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: needs re-comparison against new 816 baseline. Owes `-0.390625` (1000→999.609375) at halved step next — next constant up.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.0046875` (3.0→2.9953125) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: eight exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+0.703125` (855→855.703125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: first exact tie found (+8); owes `-4` (800→796) at halved step next.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — at float precision floor. Owes `-0.0003125` (0.22→0.2196875) next.
- `TARGET_BLEND_DIST_NORM` = 3030 (accepted, committed `fab80fd`). Owes `+37.5` (3030→3067.5) next.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0. Owes `-0.005` (1.0→0.995) at halved step next.
- **Current best cost: 816**. Four acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`), run 107 (`fab80fd`). Streak: 3 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 109)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `THRUST_ZERO_ANGLE_DEG` +1 rejected (849 vs 816) — Long Straight lost turn-count and boost effectiveness.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step — next constant up.
- `BRAKING_ZONE_MIN`: needs re-comparison against new 816 baseline. Owes `-0.390625` (1000→999.609375) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.0046875` (3.0→2.9953125) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: eight exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+0.703125` (855→855.703125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: first exact tie found (+8); owes `-4` (800→796) at halved step next.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — at float precision floor. Owes `-0.0003125` (0.22→0.2196875) next.
- `TARGET_BLEND_DIST_NORM` = 3030 (accepted, committed `fab80fd`). Owes `+37.5` (3030→3067.5) next.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, reconfirmed against new baseline too. Owes `-0.005` (1.0→0.995) at halved step next.
- **Current best cost: 816**. Four acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`), run 107 (`fab80fd`). Streak: 2 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 108)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `DRIFT_CORRECTION_FACTOR` +0.01 rejected against the new 816 baseline (966 vs 816) — Simple Oval and Long Straight lost badly despite B winning more raw metrics. 1.0 remains confirmed correct.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step — next constant up.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: five close rejections (832, 833, 830, 837, 832) against the old 829 baseline — needs re-comparison against new 816 baseline. Owes `-0.390625` (1000→999.609375) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.0046875` (3.0→2.9953125) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: eight exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+0.703125` (855→855.703125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: first exact tie found (+8); owes `-4` (800→796) at halved step next.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — at float precision floor. Owes `-0.0003125` (0.22→0.2196875) next.
- `TARGET_BLEND_DIST_NORM` = 3030 (accepted, committed `fab80fd`). Owes `+37.5` (3030→3067.5) next.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, reconfirmed against new baseline too. Owes `-0.005` (1.0→0.995) at halved step next.
- **Current best cost: 816**. Four acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`), run 107 (`fab80fd`). Streak: 1 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 107) — FOURTH ACCEPTANCE

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- **Fourth win**: `TARGET_BLEND_DIST_NORM` 3000→3030 dropped cost 829→816 — Simple Oval and Long Straight's boost effectiveness both improved, Mixed's flipped slightly positive, at the cost of a moderate Hairpin loss. Committed as `fab80fd`.
- Auto-commit-on-acceptance worked correctly again: diff verified single-constant, committed, confirmed clean.

## What failed
- Nothing failed.

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: five close rejections now (832, 833, 830, 837, 832). Owes `-0.390625` (1000→999.609375) at halved step next — recompare against new 816 baseline.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.0046875` (3.0→2.9953125) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: eight exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+0.703125` (855→855.703125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: first exact tie found (+8); owes `-4` (800→796) at halved step next.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — at float precision floor. Owes `-0.0003125` (0.22→0.2196875) next.
- `TARGET_BLEND_DIST_NORM` = **3030 (accepted, committed `fab80fd`)**. All future runs compare against cost 816, not 829. Owes `+37.5` (3030→3067.5) at same step next (accepting direction keeps its `next_dir` — step wasn't halved on acceptance).
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, five probed offsets rejected on both sides. Owes `+0.003125` (1.0→1.003125) next visit — next constant up.
- **Current best cost: 816** (was 829 for 57 runs). Four acceptances total now: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`), run 107 (`fab80fd`). Streak reset to 0.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 106)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `TARGET_BLEND_MAX_WEIGHT` -0.0025 rejected (903 vs 829) — B won turn-count on 2 tracks but boost effectiveness losses dominate. 0.22 reconfirmed better. Step halves to 0.0003125.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: five close rejections now (832, 833, 830, 837, 832). Owes `-0.390625` (1000→999.609375) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.0046875` (3.0→2.9953125) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: eight exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+0.703125` (855→855.703125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: first exact tie found (+8); owes `-4` (800→796) at halved step next.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — at float precision floor now. Owes `-0.0003125` (0.22→0.2196875) next — next constant up.
- `TARGET_BLEND_DIST_NORM`: owes `+4.6875` (3000→3004.6875) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, five probed offsets rejected on both sides. Owes `+0.003125` (1.0→1.003125) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 57 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 105)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `SHIELD_PREDICTED_DIST_TRIGGER` +8 an exact tie — first tie for this constant at this fine step size. Approaching insensitivity/convergence, similar to `BOOST_DIST_MIN`'s pattern.

## What failed
- Nothing failed; nudge rejected (expected outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: five close rejections now (832, 833, 830, 837, 832). Owes `-0.390625` (1000→999.609375) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.0046875` (3.0→2.9953125) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: eight exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+0.703125` (855→855.703125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: first exact tie found (+8); owes `-4` (800→796) at halved step next — next constant up.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — approaching float precision limits. Owes `-0.000625` (0.22→0.219375) next.
- `TARGET_BLEND_DIST_NORM`: owes `+4.6875` (3000→3004.6875) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, five probed offsets rejected on both sides. Owes `+0.003125` (1.0→1.003125) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 56 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 104)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `SHIELD_DIST_TRIGGER` -11.25 rejected (918 vs 829) — Long Straight lost its boost, no DNF this time. Step halves to 0.703125.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: five close rejections now (832, 833, 830, 837, 832). Owes `-0.390625` (1000→999.609375) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.0046875` (3.0→2.9953125) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: eight exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+0.703125` (855→855.703125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-0.625` (800→799.375) at halved step — next constant up.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — approaching float precision limits. Owes `-0.000625` (0.22→0.219375) next.
- `TARGET_BLEND_DIST_NORM`: owes `+4.6875` (3000→3004.6875) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, five probed offsets rejected on both sides. Owes `+0.003125` (1.0→1.003125) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 55 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 103)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BOOST_DIST_MIN` confirming pass-through — eighth exact tie. Completely converged.

## What failed
- Nothing failed; nudge rejected (expected outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: five close rejections now (832, 833, 830, 837, 832). Owes `-0.390625` (1000→999.609375) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.0046875` (3.0→2.9953125) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: **eight exact ties now** — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-1.40625` (855→853.59375) at halved step next — next constant up.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-0.625` (800→799.375) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — approaching float precision limits. Owes `-0.000625` (0.22→0.219375) next.
- `TARGET_BLEND_DIST_NORM`: owes `+4.6875` (3000→3004.6875) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, five probed offsets rejected on both sides. Owes `+0.003125` (1.0→1.003125) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 54 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 102)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BOOST_ANGLE_MAX_DEG` confirming pass-through — exact 16/16 tie, as expected.

## What failed
- Nothing failed; nudge rejected (expected outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: five close rejections now (832, 833, 830, 837, 832). Owes `-0.390625` (1000→999.609375) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.0046875` (3.0→2.9953125) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: seven exact ties — completely converged — next constant up, expect another quick pass-through.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-1.40625` (855→853.59375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-0.625` (800→799.375) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — approaching float precision limits. Owes `-0.000625` (0.22→0.219375) next.
- `TARGET_BLEND_DIST_NORM`: owes `+4.6875` (3000→3004.6875) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, five probed offsets rejected on both sides. Owes `+0.003125` (1.0→1.003125) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 53 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 101)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BRAKING_THRUST_CAP` +1 rejected (1056 vs 829) — same pattern as runs 53/65/77/89. 40 remains fully converged.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: five close rejections now (832, 833, 830, 837, 832). Owes `-0.390625` (1000→999.609375) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.0046875` (3.0→2.9953125) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right) — next constant up, expect quick pass-through.
- `BOOST_DIST_MIN`: seven exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-1.40625` (855→853.59375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-0.625` (800→799.375) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — approaching float precision limits. Owes `-0.000625` (0.22→0.219375) next.
- `TARGET_BLEND_DIST_NORM`: owes `+4.6875` (3000→3004.6875) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, five probed offsets rejected on both sides. Owes `+0.003125` (1.0→1.003125) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 52 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 100) — 100TH RUN MILESTONE

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BRAKING_ZONE_SPEED_MULT` +0.03 rejected hard (957 vs 829) — Simple Oval devastated again. Step halves to 0.0046875.
- **100th run milestone**: three acceptances total (run 10, 44, 49), cost improved from 879→829 (5.7%). See output file for full milestone summary.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — both ±1.125 rejected by exactly 1 point each. Owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision now; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: five close rejections now (832, 833, 830, 837, 832). Owes `-0.390625` (1000→999.609375) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe at every step; owes `-0.0046875` (3.0→2.9953125) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor — next constant up.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: seven exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-1.40625` (855→853.59375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-0.625` (800→799.375) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — approaching float precision limits. Owes `-0.000625` (0.22→0.219375) next.
- `TARGET_BLEND_DIST_NORM`: owes `+4.6875` (3000→3004.6875) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, five probed offsets rejected on both sides. Owes `+0.003125` (1.0→1.003125) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 51 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 99)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BRAKING_ZONE_MIN` +10 rejected, still close (832 vs 829) — small losses on Hairpin and Mixed only. Step halves to 0.390625.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — both ±1.125 rejected by exactly 1 point each, very likely fully converged. Owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision now; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: five close rejections now (832, 833, 830, 837, 832). Owes `-0.390625` (1000→999.609375) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: rejected by just 1 point at halved step; owes `+0.0046875` (3.0→3.0046875) next — next constant up.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: seven exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-1.40625` (855→853.59375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-0.625` (800→799.375) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — approaching float precision limits. Owes `-0.000625` (0.22→0.219375) next.
- `TARGET_BLEND_DIST_NORM`: owes `+4.6875` (3000→3004.6875) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, five probed offsets rejected on both sides. Owes `+0.003125` (1.0→1.003125) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 50 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 98)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `THRUST_ANGLE_FACTOR` +0.007 rejected (954 vs 829) — Hairpin won but Simple Oval and Long Straight lost badly. Step halves to 0.00109375, essentially at the floor of meaningful precision.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — both ±1.125 rejected by exactly 1 point each, very likely fully converged. Owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: at the edge of meaningful precision now; owes `-0.00109375` (0.7→0.69890625) at halved step.
- `BRAKING_ZONE_MIN`: four close rejections now (832, 833, 830, 837). Owes `+0.78125` (1000→1000.78125) at halved step next — next constant up.
- `BRAKING_ZONE_SPEED_MULT`: rejected by just 1 point at halved step; owes `+0.0046875` (3.0→3.0046875) next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: seven exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-1.40625` (855→853.59375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-0.625` (800→799.375) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — approaching float precision limits. Owes `-0.000625` (0.22→0.219375) next.
- `TARGET_BLEND_DIST_NORM`: owes `+4.6875` (3000→3004.6875) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, five probed offsets rejected on both sides. Owes `+0.003125` (1.0→1.003125) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 49 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 97)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `THRUST_ZERO_ANGLE_DEG` -1.125 rejected by 1 point (830 vs 829) — identical mechanism to run 73's rejection (Hairpin boost effectiveness 0 vs -1).

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — both ±1.125 now rejected by exactly 1 point each. Owes `+0.5625` (92→92.5625) at halved step next — very likely fully converged at 92.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.0021875` (0.7→0.6978125) at halved step — next constant up.
- `BRAKING_ZONE_MIN`: four close rejections now (832, 833, 830, 837). Owes `+0.78125` (1000→1000.78125) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: rejected by just 1 point at halved step; owes `+0.0046875` (3.0→3.0046875) next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: seven exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-1.40625` (855→853.59375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-0.625` (800→799.375) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — approaching float precision limits. Owes `-0.000625` (0.22→0.219375) next.
- `TARGET_BLEND_DIST_NORM`: owes `+4.6875` (3000→3004.6875) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, five probed offsets rejected on both sides. Owes `+0.003125` (1.0→1.003125) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 48 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 96) — CYCLE CLOSED

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `DRIFT_CORRECTION_FACTOR` -0.0125 rejected hard (1081 vs 829) — Simple Oval and Hairpin both devastated. 1.0 remains confirmed. Closes another full cycle with zero new acceptances (three total remain: run 10, 44, 49).

## What failed
- Nothing failed; nudge rejected (normal outcome, if severe).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step — next constant up, new cycle.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.0021875` (0.7→0.6978125) at halved step.
- `BRAKING_ZONE_MIN`: four close rejections now (832, 833, 830, 837). Owes `+0.78125` (1000→1000.78125) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: rejected by just 1 point at halved step; owes `+0.0046875` (3.0→3.0046875) next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: seven exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-1.40625` (855→853.59375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-0.625` (800→799.375) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — approaching float precision limits. Owes `-0.000625` (0.22→0.219375) next.
- `TARGET_BLEND_DIST_NORM`: owes `+4.6875` (3000→3004.6875) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, five probed offsets rejected on both sides. Owes `+0.003125` (1.0→1.003125) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 47 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 95)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `TARGET_BLEND_DIST_NORM` -37.5 rejected (898 vs 829) — even 3/3 split on raw metrics but Long Straight's boost effectiveness loss (78→24) dominates. Step halves to 4.6875.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.0021875` (0.7→0.6978125) at halved step.
- `BRAKING_ZONE_MIN`: four close rejections now (832, 833, 830, 837). Owes `+0.78125` (1000→1000.78125) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: rejected by just 1 point at halved step; owes `+0.0046875` (3.0→3.0046875) next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: seven exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-1.40625` (855→853.59375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-0.625` (800→799.375) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — approaching float precision limits. Owes `-0.000625` (0.22→0.219375) next.
- `TARGET_BLEND_DIST_NORM`: owes `+4.6875` (3000→3004.6875) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, all four probed offsets rejected on both sides. Owes `-0.00625` (1.0→0.99375) next visit — next constant up.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 46 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 94)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `TARGET_BLEND_MAX_WEIGHT` +0.0025 rejected (869 vs 829) despite B slightly ahead on raw metrics — Long Straight's boost effectiveness loss (78→27) dominates. Step halves to 0.000625, nearing float precision limits.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.0021875` (0.7→0.6978125) at halved step.
- `BRAKING_ZONE_MIN`: four close rejections now (832, 833, 830, 837). Owes `+0.78125` (1000→1000.78125) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: rejected by just 1 point at halved step; owes `+0.0046875` (3.0→3.0046875) next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: seven exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-1.40625` (855→853.59375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-0.625` (800→799.375) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — approaching float precision limits on step size; owes `-0.000625` (0.22→0.219375) next.
- `TARGET_BLEND_DIST_NORM`: owes `-9.375` (3000→2990.625) at halved step — next constant up.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, all four probed offsets rejected on both sides. Owes `-0.00625` (1.0→0.99375) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 45 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 93)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `SHIELD_PREDICTED_DIST_TRIGGER` -10 rejected (915 vs 829) — Long Straight lost its boost entirely again, everything else exact tie. Step halves to 0.625.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.0021875` (0.7→0.6978125) at halved step.
- `BRAKING_ZONE_MIN`: four close rejections now (832, 833, 830, 837). Owes `+0.78125` (1000→1000.78125) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: rejected by just 1 point at halved step; owes `+0.0046875` (3.0→3.0046875) next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: seven exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-1.40625` (855→853.59375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-0.625` (800→799.375) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — reconfirmed strictly better than both 0.215 and 0.225. Owes `-0.00125` (0.22→0.21875) next — next constant up.
- `TARGET_BLEND_DIST_NORM`: owes `-9.375` (3000→2990.625) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, all four probed offsets rejected on both sides. Owes `-0.00625` (1.0→0.99375) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 44 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 92)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `SHIELD_DIST_TRIGGER` +22.5 rejected hard (1033 vs 829) — Long Straight's boost effectiveness collapsed to -54 (no DNF). Reconfirms 855 as decisively better than moving back toward the original 900. Step halves to 1.40625.

## What failed
- Nothing failed; nudge rejected (normal outcome, if severe).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.0021875` (0.7→0.6978125) at halved step.
- `BRAKING_ZONE_MIN`: four close rejections now (832, 833, 830, 837). Owes `+0.78125` (1000→1000.78125) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: rejected by just 1 point at halved step; owes `+0.0046875` (3.0→3.0046875) next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: seven exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `-1.40625` (855→853.59375) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+1.25` (800→801.25) at halved step — next constant up.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — reconfirmed strictly better than both 0.215 and 0.225. Owes `-0.00125` (0.22→0.21875) next.
- `TARGET_BLEND_DIST_NORM`: owes `-9.375` (3000→2990.625) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, all four probed offsets rejected on both sides. Owes `-0.00625` (1.0→0.99375) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 43 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 91)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BOOST_DIST_MIN` confirming pass-through — seventh exact tie. Completely converged.

## What failed
- Nothing failed; nudge rejected (expected outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.0021875` (0.7→0.6978125) at halved step.
- `BRAKING_ZONE_MIN`: four close rejections now (832, 833, 830, 837). Owes `+0.78125` (1000→1000.78125) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: rejected by just 1 point at halved step; owes `+0.0046875` (3.0→3.0046875) next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: **seven exact ties now** — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+2.8125` (855→857.8125) at halved step next — next constant up.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+1.25` (800→801.25) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — reconfirmed strictly better than both 0.215 and 0.225. Owes `-0.00125` (0.22→0.21875) next.
- `TARGET_BLEND_DIST_NORM`: owes `-9.375` (3000→2990.625) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, all four probed offsets rejected on both sides. Owes `-0.00625` (1.0→0.99375) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 42 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 90)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BOOST_ANGLE_MAX_DEG` confirming pass-through — exact 16/16 tie, as expected.

## What failed
- Nothing failed; nudge rejected (expected outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.0021875` (0.7→0.6978125) at halved step.
- `BRAKING_ZONE_MIN`: four close rejections now (832, 833, 830, 837). Owes `+0.78125` (1000→1000.78125) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: rejected by just 1 point at halved step; owes `+0.0046875` (3.0→3.0046875) next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: six exact ties — completely converged — next constant up, expect another quick pass-through.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+2.8125` (855→857.8125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+1.25` (800→801.25) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — reconfirmed strictly better than both 0.215 and 0.225. Owes `-0.00125` (0.22→0.21875) next.
- `TARGET_BLEND_DIST_NORM`: owes `-9.375` (3000→2990.625) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, all four probed offsets rejected on both sides. Owes `-0.00625` (1.0→0.99375) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 41 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 89)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BRAKING_THRUST_CAP` -1 rejected hard (1118 vs 829), identical result to run 65 — Simple Oval's boost delayed to turn 240. 40 remains a very solid, fully converged optimum.

## What failed
- Nothing failed; nudge rejected (normal outcome, if severe).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.0021875` (0.7→0.6978125) at halved step.
- `BRAKING_ZONE_MIN`: four close rejections now (832, 833, 830, 837). Owes `+0.78125` (1000→1000.78125) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: rejected by just 1 point at halved step; owes `+0.0046875` (3.0→3.0046875) next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right) — next constant up, expect quick pass-through.
- `BOOST_DIST_MIN`: six exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+2.8125` (855→857.8125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+1.25` (800→801.25) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — reconfirmed strictly better than both 0.215 and 0.225. Owes `-0.00125` (0.22→0.21875) next.
- `TARGET_BLEND_DIST_NORM`: owes `-9.375` (3000→2990.625) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, all four probed offsets rejected on both sides. Owes `-0.00625` (1.0→0.99375) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 40 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 88)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BRAKING_ZONE_SPEED_MULT` -0.0375 rejected by just 1 point (830 vs 829) — a single turn of Mixed's boost effectiveness is the entire margin. Step halves to 0.0046875.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.0021875` (0.7→0.6978125) at halved step.
- `BRAKING_ZONE_MIN`: four close rejections now (832, 833, 830, 837). Owes `+0.78125` (1000→1000.78125) at halved step next.
- `BRAKING_ZONE_SPEED_MULT`: rejected by just 1 point at halved step; owes `+0.0046875` (3.0→3.0046875) next.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor — next constant up.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: six exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+2.8125` (855→857.8125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+1.25` (800→801.25) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — reconfirmed strictly better than both 0.215 and 0.225. Owes `-0.00125` (0.22→0.21875) next.
- `TARGET_BLEND_DIST_NORM`: owes `-9.375` (3000→2990.625) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, all four probed offsets rejected on both sides. Owes `-0.00625` (1.0→0.99375) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 39 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 87)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BRAKING_ZONE_MIN` -12.5 rejected, still a close margin (837 vs 829) — small losses on Simple Oval and Hairpin only. Step halves to 0.78125.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.0021875` (0.7→0.6978125) at halved step.
- `BRAKING_ZONE_MIN`: four close rejections now (832, 833, 830, 837). Owes `+0.78125` (1000→1000.78125) at halved step next — this constant's search has narrowed considerably without crossing over.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.009375` (3.0→2.990625) at halved step — next constant up.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: six exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+2.8125` (855→857.8125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+1.25` (800→801.25) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — reconfirmed strictly better than both 0.215 and 0.225. Owes `-0.00125` (0.22→0.21875) next.
- `TARGET_BLEND_DIST_NORM`: owes `-9.375` (3000→2990.625) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, all four probed offsets rejected on both sides. Owes `-0.00625` (1.0→0.99375) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 38 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 86)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `THRUST_ANGLE_FACTOR` -0.00875 rejected, clean sweep (983 vs 829). 0.7 remains extremely sharp at every step size tried. Step halves to 0.0021875.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.0021875` (0.7→0.6978125) at halved step.
- `BRAKING_ZONE_MIN`: three razor-thin rejections now (832, 833, 830). Owes `-1.5625` (1000→998.4375) at halved step next — worth continued close attention — next constant up.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.009375` (3.0→2.990625) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: six exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+2.8125` (855→857.8125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+1.25` (800→801.25) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — reconfirmed strictly better than both 0.215 and 0.225. Owes `-0.00125` (0.22→0.21875) next.
- `TARGET_BLEND_DIST_NORM`: owes `-9.375` (3000→2990.625) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, all four probed offsets rejected on both sides. Owes `-0.00625` (1.0→0.99375) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 37 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 85)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `THRUST_ZERO_ANGLE_DEG` +1.125 rejected (858 vs 829) — Long Straight lost turn-count and boost effectiveness, everything else tied. Step halves to 0.5625.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `+0.5625` (92→92.5625) at halved step.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.004375` (0.7→0.695625) at halved step — next constant up.
- `BRAKING_ZONE_MIN`: three razor-thin rejections now (832, 833, 830). Owes `-1.5625` (1000→998.4375) at halved step next — worth continued close attention.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.009375` (3.0→2.990625) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: six exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+2.8125` (855→857.8125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+1.25` (800→801.25) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — reconfirmed strictly better than both 0.215 and 0.225. Owes `-0.00125` (0.22→0.21875) next.
- `TARGET_BLEND_DIST_NORM`: owes `-9.375` (3000→2990.625) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, all four probed offsets rejected on both sides. Owes `-0.00625` (1.0→0.99375) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 36 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 84) — CYCLE CLOSED

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `DRIFT_CORRECTION_FACTOR` +0.0125 rejected hard (1067 vs 829) — Simple Oval devastated again, clean sweep for the best. 1.0 remains the confirmed sharp optimum. This closes another full round-robin cycle with zero new acceptances (three total remain from the whole search: run 10, 44, 49).

## What failed
- Nothing failed; nudge rejected (normal outcome, if severe).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `-1.125` (92→90.875) at halved step — next constant up, new cycle.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.004375` (0.7→0.695625) at halved step.
- `BRAKING_ZONE_MIN`: three razor-thin rejections now (832, 833, 830). Owes `-1.5625` (1000→998.4375) at halved step next — worth continued close attention.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.009375` (3.0→2.990625) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: six exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+2.8125` (855→857.8125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+1.25` (800→801.25) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — reconfirmed strictly better than both 0.215 and 0.225. Owes `-0.00125` (0.22→0.21875) next.
- `TARGET_BLEND_DIST_NORM`: owes `-9.375` (3000→2990.625) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, all four probed offsets rejected on both sides. Owes `-0.00625` (1.0→0.99375) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 35 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 83)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `TARGET_BLEND_DIST_NORM` +37.5 rejected (914 vs 829) — Long Straight's boost effectiveness collapsed (78→4), dominating despite B winning turn-count on 2 tracks. Step halves to 9.375.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `-1.125` (92→90.875) at halved step.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.004375` (0.7→0.695625) at halved step.
- `BRAKING_ZONE_MIN`: three razor-thin rejections now (832, 833, 830). Owes `-1.5625` (1000→998.4375) at halved step next — worth continued close attention.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.009375` (3.0→2.990625) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: six exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+2.8125` (855→857.8125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+1.25` (800→801.25) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — reconfirmed strictly better than both 0.215 and 0.225. Owes `-0.00125` (0.22→0.21875) next.
- `TARGET_BLEND_DIST_NORM`: owes `-9.375` (3000→2990.625) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, both ±0.05, ±0.025 rejected on both sides. Owes `+0.00625` (1.0→1.00625) next visit — next constant up.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 34 since last acceptance.
- Long Straight fragility pattern at 7+ occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 82)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `TARGET_BLEND_MAX_WEIGHT` -0.005 rejected (966 vs 829) — B won turn-count on 3/4 tracks but lost boost effectiveness heavily (Hairpin 0→-39). Reconfirms 0.22 as strictly better.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `-1.125` (92→90.875) at halved step.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.004375` (0.7→0.695625) at halved step.
- `BRAKING_ZONE_MIN`: three razor-thin rejections now (832, 833, 830). Owes `-1.5625` (1000→998.4375) at halved step next — worth continued close attention.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.009375` (3.0→2.990625) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: six exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+2.8125` (855→857.8125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+1.25` (800→801.25) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — reconfirmed strictly better than both 0.215 and 0.225. Owes `-0.00125` (0.22→0.21875) next.
- `TARGET_BLEND_DIST_NORM`: owes `+18.75` (3000→3018.75) at halved step — next constant up.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, both ±0.05, ±0.025 rejected on both sides. Owes `+0.00625` (1.0→1.00625) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 33 since last acceptance.
- Long Straight fragility pattern at 7 occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 81)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `SHIELD_PREDICTED_DIST_TRIGGER` +10 rejected by a small margin (836 vs 829) — Hairpin's boost effectiveness dips, everything else tied. Step halves to 1.25.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `-1.125` (92→90.875) at halved step.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.004375` (0.7→0.695625) at halved step.
- `BRAKING_ZONE_MIN`: three razor-thin rejections now (832, 833, 830). Owes `-1.5625` (1000→998.4375) at halved step next — worth continued close attention.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.009375` (3.0→2.990625) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: six exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+2.8125` (855→857.8125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `+1.25` (800→801.25) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — `+0.005` also rejected. Owes `+0.00125` (0.22→0.22125) next — next constant up.
- `TARGET_BLEND_DIST_NORM`: owes `+18.75` (3000→3018.75) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, both ±0.05, ±0.025 rejected on both sides. Owes `+0.00625` (1.0→1.00625) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 32 since last acceptance.
- Long Straight fragility pattern at 7 occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 80)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `SHIELD_DIST_TRIGGER` -22.5 rejected (931 vs 829) — Long Straight lost its boost and slipped turn-count, no DNF this time. Step halves to 2.8125.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `-1.125` (92→90.875) at halved step.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.004375` (0.7→0.695625) at halved step.
- `BRAKING_ZONE_MIN`: three razor-thin rejections now (832, 833, 830). Owes `-1.5625` (1000→998.4375) at halved step next — worth continued close attention.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.009375` (3.0→2.990625) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: six exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — owes `+2.8125` (855→857.8125) at halved step next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-2.5` (800→797.5) at halved step — next constant up.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — `+0.005` also rejected. Owes `+0.00125` (0.22→0.22125) next.
- `TARGET_BLEND_DIST_NORM`: owes `+18.75` (3000→3018.75) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, both ±0.05, ±0.025 rejected on both sides. Owes `+0.00625` (1.0→1.00625) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 31 since last acceptance.
- Long Straight fragility pattern at 7 occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 79)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BOOST_DIST_MIN` confirming pass-through — sixth exact tie. Fully converged.

## What failed
- Nothing failed; nudge rejected (expected outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `-1.125` (92→90.875) at halved step.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.004375` (0.7→0.695625) at halved step.
- `BRAKING_ZONE_MIN`: three razor-thin rejections now (832, 833, 830). Owes `-1.5625` (1000→998.4375) at halved step next — worth continued close attention.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.009375` (3.0→2.990625) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: **six exact ties now** — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — re-confirmed better than 900 too. Owes `-5.625` (855→849.375) next — next constant up.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-2.5` (800→797.5) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — `+0.005` also rejected. Owes `+0.00125` (0.22→0.22125) next.
- `TARGET_BLEND_DIST_NORM`: owes `+18.75` (3000→3018.75) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, both ±0.05, ±0.025 rejected on both sides. Owes `+0.00625` (1.0→1.00625) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 30 since last acceptance.
- Long Straight fragility pattern at 7 occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 78)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BOOST_ANGLE_MAX_DEG` confirming pass-through — exact 16/16 tie, as expected.

## What failed
- Nothing failed; nudge rejected (expected outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `-1.125` (92→90.875) at halved step.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.004375` (0.7→0.695625) at halved step.
- `BRAKING_ZONE_MIN`: three razor-thin rejections now (832, 833, 830). Owes `-1.5625` (1000→998.4375) at halved step next — worth continued close attention.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.009375` (3.0→2.990625) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: five exact ties — completely converged — next constant up, expect another quick pass-through.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — re-confirmed better than 900 too. Owes `-5.625` (855→849.375) next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-2.5` (800→797.5) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — `+0.005` also rejected. Owes `+0.00125` (0.22→0.22125) next.
- `TARGET_BLEND_DIST_NORM`: owes `+18.75` (3000→3018.75) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, both ±0.05, ±0.025 rejected on both sides. Owes `+0.00625` (1.0→1.00625) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 29 since last acceptance.
- Long Straight fragility pattern at 7 occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 77)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BRAKING_THRUST_CAP` +1 rejected (1056 vs 829) — Simple Oval's blowup dominates despite Hairpin/Mixed wins. 40 continues to be a very sharp, protected optimum.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `-1.125` (92→90.875) at halved step.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.004375` (0.7→0.695625) at halved step.
- `BRAKING_ZONE_MIN`: three razor-thin rejections now (832, 833, 830). Owes `-1.5625` (1000→998.4375) at halved step next — worth continued close attention.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.009375` (3.0→2.990625) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right) — next constant up, expect quick pass-through.
- `BOOST_DIST_MIN`: five exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — re-confirmed better than 900 too. Owes `-5.625` (855→849.375) next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-2.5` (800→797.5) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — `+0.005` also rejected. Owes `+0.00125` (0.22→0.22125) next.
- `TARGET_BLEND_DIST_NORM`: owes `+18.75` (3000→3018.75) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, both ±0.05, ±0.025 rejected on both sides. Owes `+0.00625` (1.0→1.00625) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 28 since last acceptance.
- Long Straight fragility pattern at 7 occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 76)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BRAKING_ZONE_SPEED_MULT` +0.0375 rejected hard (957 vs 829) — Simple Oval devastated again. `+` direction remains consistently severe at every step. Step halves to 0.009375.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `-1.125` (92→90.875) at halved step.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.004375` (0.7→0.695625) at halved step.
- `BRAKING_ZONE_MIN`: three razor-thin rejections now (832, 833, 830). Owes `-1.5625` (1000→998.4375) at halved step next — worth continued close attention.
- `BRAKING_ZONE_SPEED_MULT`: `+` direction consistently severe; owes `-0.009375` (3.0→2.990625) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor — next constant up.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: five exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — re-confirmed better than 900 too. Owes `-5.625` (855→849.375) next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-2.5` (800→797.5) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — `+0.005` also rejected. Owes `+0.00125` (0.22→0.22125) next.
- `TARGET_BLEND_DIST_NORM`: owes `+18.75` (3000→3018.75) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, both ±0.05, ±0.025 rejected on both sides. Owes `+0.00625` (1.0→1.00625) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 27 since last acceptance.
- Long Straight fragility pattern at 7 occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 75)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `BRAKING_ZONE_MIN` +12.5 rejected again by just 1 point (830 vs 829) — the third razor-thin rejection for this constant across both directions. 1000 sits in an extremely narrow, near-flat local optimum. Step halves to 1.5625.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `-1.125` (92→90.875) at halved step.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.004375` (0.7→0.695625) at halved step.
- `BRAKING_ZONE_MIN`: **three razor-thin rejections now** (832, 833, 830) — 1000 sits at an extremely narrow local optimum. Owes `-1.5625` (1000→998.4375) at halved step next — worth continued close attention, this is the closest this search has come to a fourth win.
- `BRAKING_ZONE_SPEED_MULT`: owes `+0.01875` (3.0→3.01875) at halved step — next constant up.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: five exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — re-confirmed better than 900 too. Owes `-5.625` (855→849.375) next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-2.5` (800→797.5) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — `+0.005` also rejected. Owes `+0.00125` (0.22→0.22125) next.
- `TARGET_BLEND_DIST_NORM`: owes `+18.75` (3000→3018.75) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, both ±0.05, ±0.025 rejected on both sides. Owes `+0.00625` (1.0→1.00625) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 26 since last acceptance.
- Long Straight fragility pattern at 7 occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 74)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `THRUST_ANGLE_FACTOR` +0.00875 rejected hard (1059 vs 829) — Simple Oval collapsed again (192→306). This constant remains extremely sharp at 0.7 regardless of step size — likely as converged as any constant in this search.

## What failed
- Nothing failed; nudge rejected (normal outcome, if severe).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `-1.125` (92→90.875) at halved step.
- `THRUST_ANGLE_FACTOR`: extremely consistently sharp at 0.7; owes `-0.004375` (0.7→0.695625) at halved step.
- `BRAKING_ZONE_MIN`: two tightest-margin rejections of the whole search, both directions (832, 833). Owes `-3.125` (1000→996.875) at halved step — deserves continued close attention, next constant up.
- `BRAKING_ZONE_SPEED_MULT`: owes `+0.01875` (3.0→3.01875) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: five exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — re-confirmed better than 900 too. Owes `-5.625` (855→849.375) next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-2.5` (800→797.5) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — `+0.005` also rejected. Owes `+0.00125` (0.22→0.22125) next.
- `TARGET_BLEND_DIST_NORM`: owes `+18.75` (3000→3018.75) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, both ±0.05, ±0.025 rejected on both sides. Owes `+0.00625` (1.0→1.00625) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 25 since last acceptance.
- Long Straight fragility pattern at 7 occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 73)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `THRUST_ZERO_ANGLE_DEG` -2.25 rejected by an extremely tight margin (830 vs 829) — a single point of Hairpin boost effectiveness is the entire difference. Step halves to 1.125.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`) — owes `-1.125` (92→90.875) at halved step.
- `THRUST_ANGLE_FACTOR`: near-fully converged at 0.7, step down to 0.0021875 — next constant up.
- `BRAKING_ZONE_MIN`: two tightest-margin rejections of the whole search, both directions (832, 833). Owes `-3.125` (1000→996.875) at halved step — deserves continued close attention.
- `BRAKING_ZONE_SPEED_MULT`: owes `+0.01875` (3.0→3.01875) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: five exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — re-confirmed better than 900 too. Owes `-5.625` (855→849.375) next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-2.5` (800→797.5) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — `+0.005` also rejected. Owes `+0.00125` (0.22→0.22125) next.
- `TARGET_BLEND_DIST_NORM`: owes `+18.75` (3000→3018.75) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, both ±0.05, ±0.025 rejected on both sides. Owes `+0.00625` (1.0→1.00625) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 24 since last acceptance.
- Long Straight fragility pattern at 7 occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 72) — CYCLE CLOSED

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `DRIFT_CORRECTION_FACTOR` -0.025 rejected (942 vs 829) — Simple Oval devastated again. 1.0 keeps reconfirming as the correct optimum at every step size tried. Step halves to 0.00625. This closes another full round-robin cycle with zero new acceptances (three total remain: run 10, 44, 49).

## What failed
- Nothing failed; nudge rejected (normal outcome, if severe).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`). Owes `+0.5625` (92→92.5625) next visit — first constant of new cycle.
- `THRUST_ANGLE_FACTOR`: near-fully converged at 0.7, step down to 0.0021875.
- `BRAKING_ZONE_MIN`: two tightest-margin rejections of the whole search, both directions (832, 833). Owes `-3.125` (1000→996.875) at halved step — deserves continued close attention.
- `BRAKING_ZONE_SPEED_MULT`: owes `+0.01875` (3.0→3.01875) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: five exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — re-confirmed better than 900 too. Owes `-5.625` (855→849.375) next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-2.5` (800→797.5) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — `+0.005` also rejected. Owes `+0.00125` (0.22→0.22125) next.
- `TARGET_BLEND_DIST_NORM`: owes `+18.75` (3000→3018.75) at halved step.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, both ±0.05, ±0.025 rejected on both sides. Owes `+0.00625` (1.0→1.00625) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 23 since last acceptance.
- Long Straight fragility pattern at 7 occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 71)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `TARGET_BLEND_DIST_NORM` -75 rejected clean sweep (1020 vs 829) — Long Straight devastated (240→323 turns), the fragility pattern's 7th occurrence. Step halves to 18.75.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`). Owes `+0.5625` (92→92.5625) next visit.
- `THRUST_ANGLE_FACTOR`: near-fully converged at 0.7, step down to 0.0021875.
- `BRAKING_ZONE_MIN`: two tightest-margin rejections of the whole search, both directions (832, 833). Owes `-3.125` (1000→996.875) at halved step — deserves continued close attention.
- `BRAKING_ZONE_SPEED_MULT`: owes `+0.01875` (3.0→3.01875) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: five exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — re-confirmed better than 900 too. Owes `-5.625` (855→849.375) next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-2.5` (800→797.5) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — `+0.005` also rejected. Owes `+0.00125` (0.22→0.22125) next.
- `TARGET_BLEND_DIST_NORM`: owes `+18.75` (3000→3018.75) at halved step — devastated Long Straight this run at -75.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, both ±0.05 and ±0.025 rejected on both sides. Owes `-0.0125` (1.0→0.9875) next visit — next constant up.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 22 since last acceptance.
- Long Straight fragility pattern now at 7 occurrences — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/`.

---

# tune-bot run — 2026-07-05 (run 70)

- Mode: OFF
- Result: done
- Steps skipped (already passed): none
- Steps re-run: none (all steps passed on first attempt)

## What worked
- `TARGET_BLEND_MAX_WEIGHT` +0.005 (0.225) rejected (847 vs 829) despite a striking Mixed win (243→222 turns, boost effectiveness -16→+3) — Hairpin and Long Straight's boost effectiveness losses dominate. Continues to confirm the `+` side cliff near 0.22. Step halves to 0.00125.

## What failed
- Nothing failed; nudge rejected (normal outcome).

## Remember next run
(carried over from 2026-07-04's final memory entry, run 69)
- `THRUST_ZERO_ANGLE_DEG` = 92 (accepted, committed `ead4ced`). Owes `+0.5625` (92→92.5625) next visit.
- `THRUST_ANGLE_FACTOR`: near-fully converged at 0.7, step down to 0.0021875.
- `BRAKING_ZONE_MIN`: two tightest-margin rejections of the whole search, both directions (832, 833). Owes `-3.125` (1000→996.875) at halved step — deserves continued close attention.
- `BRAKING_ZONE_SPEED_MULT`: owes `+0.01875` (3.0→3.01875) at halved step.
- `BRAKING_THRUST_CAP`: fully converged, sharp optimum protected on both sides at the 1-unit integer floor.
- `BOOST_ANGLE_MAX_DEG`: tie-converged (10 reconfirmed right).
- `BOOST_DIST_MIN`: five exact ties — completely converged.
- `SHIELD_DIST_TRIGGER` = 855 (accepted, committed `72aab82`) — re-confirmed better than 900 too. Owes `-5.625` (855→849.375) next.
- `SHIELD_PREDICTED_DIST_TRIGGER`: owes `-2.5` (800→797.5) at halved step.
- `TARGET_BLEND_MAX_WEIGHT` = 0.22 (accepted, committed `82d315f`) — `+0.005` also rejected. Owes `+0.00125` (0.22→0.22125) at halved step next.
- `TARGET_BLEND_DIST_NORM`: owes `-37.5` (3000→2962.5) at halved step — next constant up.
- `DRIFT_CORRECTION_FACTOR`: sharp optimum at 1.0, both ±0.05 and ±0.025 rejected on both sides. Owes `-0.0125` (1.0→0.9875) next visit.
- **Current best cost: 829**. Three acceptances total: run 10 (`82d315f`), run 44 (`72aab82`), run 49 (`ead4ced`). Streak: 21 since last acceptance.
- Long Straight fragility pattern still at 6 DNFs/severe-collapse occurrences across the whole search — worth a manual look sometime, not urgent.
- Loop files live at `tune-bot-runs/` (relocated from `.claude/loops/tune-bot/` at run 22 of the prior session, to fix a `.claude/`-scoped permission prompt).
